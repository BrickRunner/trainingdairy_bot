# Ручной ввод дистанции для HeroLeague

## Изменение

Для соревнований от Лиги Героев пользователь теперь вводит дистанцию вручную, так как API не предоставляет структурированный список дистанций.

## Причина

API HeroLeague не возвращает структурированный список дистанций. Дистанции указаны только в текстовом описании события:
```json
{
  "description": "Лыжная гонка 1 км., Лыжная гонка 5 км., Лыжная гонка 10 км, Эстафета",
  "distances": []  // пусто!
}
```

## Решение

### Пользовательский флоу для HeroLeague:

1. Пользователь нажимает "✅ Я участвую"
2. Бот просит ввести дистанцию (текст)
3. Пользователь вводит: "5 км" / "Марафон" / "3 км (лыжная гонка)" и т.д.
4. Бот просит ввести целевое время
5. Пользователь вводит время или нажимает "Без времени"
6. Регистрация завершена

### Пользовательский флоу для RussiaRunning/Timerman:

1. Пользователь нажимает "✅ Я участвую"
2. Бот показывает список дистанций (если их несколько)
3. Пользователь выбирает дистанцию
4. Бот просит ввести целевое время
5. Регистрация завершена

## Изменения в коде

### 1. Добавлено новое состояние FSM

**Файл:** `competitions/competitions_fsm.py`

```python
class UpcomingCompetitionsStates(StatesGroup):
    # ... другие состояния
    waiting_for_custom_distance = State()  # ← НОВОЕ: Ручной ввод дистанции
    waiting_for_target_time = State()
```

### 2. Изменена логика участия в соревновании

**Файл:** `competitions/upcoming_competitions_handlers.py` (строки 740-772)

**Было:**
```python
else:
    # Нет дистанций - сохраняем без дистанции и переходим к вводу времени
    await state.update_data(selected_distance=None, selected_distance_name=None)
    await prompt_for_target_time(callback, state, comp_id)
```

**Стало:**
```python
else:
    # Нет структурированных дистанций (например, HeroLeague)
    service = comp.get('service', '')

    if service == 'HeroLeague':
        # Для HeroLeague просим пользователя ввести дистанцию вручную
        await state.set_state(UpcomingCompetitionsStates.waiting_for_custom_distance)

        message_text = (
            f"📏 <b>Введите дистанцию</b>\n\n"
            f"Укажите дистанцию, на которой вы планируете участвовать.\n\n"
            f"Примеры:\n"
            f"• 5 км\n"
            f"• 10 км\n"
            f"• 21.1 км\n"
            f"• Марафон\n"
            f"• 3 км (лыжная гонка)\n"
        )
        # ... показываем сообщение
    else:
        # Для других сервисов - старая логика
        await prompt_for_target_time(callback, state, comp_id)
```

### 3. Добавлен обработчик ввода дистанции

**Файл:** `competitions/upcoming_competitions_handlers.py` (строки 1351-1403)

```python
@router.message(UpcomingCompetitionsStates.waiting_for_custom_distance)
async def process_custom_distance(message: Message, state: FSMContext):
    """Обработать введенную дистанцию для HeroLeague"""
    user_id = message.from_user.id
    distance_name = message.text.strip()

    logger.info(f"User {user_id} entered custom distance: {distance_name}")

    # Сохраняем введенную дистанцию
    await state.update_data(
        selected_distance_name=distance_name,
        selected_distance=None  # Нет числового значения для HeroLeague
    )

    # Переходим к вводу целевого времени
    await state.set_state(UpcomingCompetitionsStates.waiting_for_target_time)

    message_text = (
        f"⏱️ <b>Введите целевое время</b>\n\n"
        f"Дистанция: {distance_name}\n\n"
        f"Укажите целевое время для этой дистанции.\n\n"
        # ... примеры
    )
    # ... показываем сообщение с кнопкой "Без времени"
```

### 4. Убрано отображение дистанций для HeroLeague

**Файл:** `competitions/upcoming_competitions_handlers.py` (строки 548-567)

**Изменение:**
```python
# Дистанции - НЕ показываем для HeroLeague
service = comp.get('service', '')

if service != 'HeroLeague':
    # Показываем дистанции только для RussiaRunning и Timerman
    if comp.get('distances'):
        text += f"\n<b>📏 Дистанции:</b>\n"
        for dist in comp['distances'][:10]:
            # ... форматируем и показываем
```

**Обоснование:** Для HeroLeague дистанции не структурированы, поэтому не имеет смысла показывать текстовое описание - пользователь все равно введет свою дистанцию.

## Пример использования

### Для пользователя (HeroLeague):

```
🏆 Гонка Героев

📅 Дата: 18.04.2026
📍 Место: Москва
🏃 Вид спорта: Бег

🔗 Подробнее на сайте

[✅ Я участвую]
[◀️ К списку]
```

После нажатия "Я участвую":

```
📏 Введите дистанцию

Укажите дистанцию, на которой вы планируете участвовать.

Примеры:
• 5 км
• 10 км
• 21.1 км
• Марафон
• 3 км (лыжная гонка)

[◀️ Назад]
```

Пользователь вводит: `10 км`

```
⏱️ Введите целевое время

Дистанция: 10 км

Укажите целевое время для этой дистанции.

Примеры:
• 1:30:05 (1 час 30 минут 5 секунд)
• 45:30 (45 минут 30 секунд)
• 1:23:45.50 (с сотыми)

Или нажмите 'Без времени' если не хотите указывать целевое время.

[⏭️ Без времени]
```

### Для пользователя (RussiaRunning/Timerman):

```
🏆 Московский марафон

📅 Дата: 20.09.2026
📍 Место: Москва
🏃 Вид спорта: Бег

📏 Дистанции:
  • 5 км
  • 10 км
  • 21.1 км (Полумарафон)
  • 42.2 км (Марафон)

[✅ Я участвую]
[◀️ К списку]
```

После нажатия "Я участвую" - показывается список дистанций с чекбоксами (как раньше).

## Преимущества

1. **Гибкость** - пользователь может указать любую дистанцию, как она написана в описании события
2. **Простота** - не нужно парсить текстовое описание или делать дополнительные API запросы
3. **Универсальность** - подход работает для любых событий без структурированных дистанций
4. **Совместимость** - не ломает существующую логику для RussiaRunning/Timerman

## Хранение данных

В базе данных дистанция сохраняется в поле `distance_name`:

```python
await add_competition_participant(
    user_id=user_id,
    competition_id=comp_id,
    distance_name="10 км",  # ← текст введенный пользователем
    distance_km=None,  # ← нет числового значения
    target_time=target_time,
    # ...
)
```

## Обратная совместимость

Изменения **полностью обратно совместимы**:
- ✅ RussiaRunning: работает как раньше (выбор из списка)
- ✅ Timerman: работает как раньше (выбор из списка)
- ✅ HeroLeague: новый флоу (ручной ввод дистанции)

## Файлы

**Измененные:**
- `competitions/competitions_fsm.py` - добавлено состояние `waiting_for_custom_distance`
- `competitions/upcoming_competitions_handlers.py` - логика ручного ввода дистанции
  - Строки 740-772: проверка сервиса и переход к вводу дистанции
  - Строки 1351-1403: обработчик ввода дистанции
  - Строки 548-567: скрытие дистанций для HeroLeague

## Будущие улучшения

1. **Автозаполнение** - предложить популярные дистанции кнопками (5 км, 10 км, 21.1 км, 42.2 км)
2. **Парсинг описания** - попытаться извлечь дистанции из текстового описания regex'ом
3. **Валидация** - проверять корректность введенной дистанции
