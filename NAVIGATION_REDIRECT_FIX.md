# Исправление редиректа после регистрации - 19 декабря 2025

## Дата: 2025-12-19

## Проблема

После регистрации на соревнование reg.place пользователя перенаправляло в раздел "Мои соревнования" вместо главного меню соревнований.

**Ожидаемое поведение:**
После регистрации пользователь должен попадать в **главное меню соревнований** (🏆 СОРЕВНОВАНИЯ) с кнопками:
- 🔍 Найти соревнования
- ✅ Мои соревнования
- 🏅 Мои результаты
- ◀️ Назад

**Фактическое поведение:**
Пользователя переносило напрямую в список "Мои соревнования".

## Решение

Изменены три функции для использования `show_competitions_menu()` вместо `show_my_competitions()`:

### 1. save_all_distances_and_redirect() (строки 1144-1188)

**Было:**
```python
from competitions.competitions_handlers import show_my_competitions
await show_my_competitions(callback_or_message, state)
```

**Стало:**
```python
from competitions.competitions_handlers import show_competitions_menu
await show_competitions_menu(callback_or_message, state)
```

### 2. skip_target_time() (строки 1404-1409)

**Было:**
```python
from competitions.competitions_handlers import show_my_competitions
await show_my_competitions(callback, state)
```

**Стало:**
```python
from competitions.competitions_handlers import show_competitions_menu
await show_competitions_menu(callback, state)
```

### 3. process_target_time() (строки 1647-1676)

**Было:**
```python
from competitions.competitions_handlers import show_my_competitions
fake_callback = FakeCallback(new_msg)
await show_my_competitions(fake_callback, state)
```

**Стало:**
```python
from competitions.competitions_handlers import show_competitions_menu
fake_callback = FakeCallback(new_msg)
fake_callback.data = "competitions"  # Добавлен callback_data
await show_competitions_menu(fake_callback, state)
```

## Результат

✅ После регистрации на соревнование (любой парсер: RussiaRunning, Timerman, HeroLeague, reg.place) пользователь **ВСЕГДА** попадает в главное меню соревнований

✅ В главном меню пользователь может выбрать:
- **"✅ Мои соревнования"** - чтобы увидеть список зарегистрированных соревнований
- **"🔍 Найти соревнования"** - чтобы найти новые соревнования
- **"🏅 Мои результаты"** - чтобы посмотреть историю
- **"◀️ Назад"** - чтобы вернуться в главное меню бота

✅ Навигация работает одинаково для всех парсеров

## Файлы изменены

**`competitions/upcoming_competitions_handlers.py`**
- **Строки 1144-1188**: `save_all_distances_and_redirect()` - изменен редирект
- **Строки 1404-1409**: `skip_target_time()` - изменен редирект
- **Строки 1647-1676**: `process_target_time()` - изменен редирект

## Тестирование

### Сценарий 1: Регистрация с одной дистанцией (RussiaRunning/Timerman)
1. Найти соревнование с одной дистанцией
2. Нажать "✅ Я участвую"
3. Ввести целевое время
4. ✅ Должно открыться главное меню "🏆 СОРЕВНОВАНИЯ"

### Сценарий 2: Регистрация с выбором дистанции (RussiaRunning/Timerman)
1. Найти соревнование с несколькими дистанциями
2. Нажать "✅ Я участвую"
3. Выбрать дистанцию
4. Ввести целевое время
5. ✅ Должно открыться главное меню "🏆 СОРЕВНОВАНИЯ"

### Сценарий 3: Регистрация с ручным вводом (HeroLeague/reg.place)
1. Найти соревнование reg.place или HeroLeague
2. Нажать "✅ Я участвую"
3. Ввести дистанцию вручную (например "10 км")
4. Ввести целевое время
5. ✅ Должно открыться главное меню "🏆 СОРЕВНОВАНИЯ"

### Сценарий 4: Пропуск целевого времени
1. Найти любое соревнование
2. Нажать "✅ Я участвую"
3. Выбрать/ввести дистанцию
4. Нажать "Пропустить" при запросе целевого времени
5. ✅ Должно открыться главное меню "🏆 СОРЕВНОВАНИЯ"

---

✅ **Проблема полностью решена!**

**Теперь навигация работает одинаково для всех парсеров (RussiaRunning, Timerman, HeroLeague, reg.place)**
