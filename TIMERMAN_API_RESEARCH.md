# Инструкция по исследованию API timerman.org

## Цель
Найти правильный API endpoint для получения списка соревнований с сайта timerman.org

## Шаги для исследования

### 1. Откройте сайт в браузере
1. Перейдите на https://timerman.org
2. Нажмите **F12** чтобы открыть DevTools (Инструменты разработчика)

### 2. Перейдите во вкладку Network
1. В DevTools выберите вкладку **Network** (Сеть)
2. Убедитесь что запись включена (красная кнопка должна гореть)
3. Можете нажать **Clear** (🚫) чтобы очистить список запросов

### 3. Найдите страницу с соревнованиями
1. Перейдите на страницу с календарем соревнований или списком событий
2. Например: https://timerman.org/calendar или https://timerman.org/events

### 4. Найдите API запрос
1. В списке запросов найдите те, которые загружают данные о соревнованиях
2. Ищите запросы типа **XHR** или **Fetch**
3. Обратите внимание на запросы которые содержат слова: `api`, `event`, `calendar`, `competition`

### 5. Изучите найденный запрос

Когда найдете нужный запрос, кликните на него и посмотрите:

#### Headers (Заголовки):
- **Request URL** - это и есть API endpoint который нам нужен!
- **Request Method** - GET или POST
- Если POST, смотрите **Request Payload** или **Form Data**

#### Payload (если POST):
Скопируйте структуру данных которые отправляются в запросе

#### Response (Ответ):
- Перейдите на вкладку **Response** или **Preview**
- Посмотрите структуру данных которые возвращает API
- Это должен быть JSON с информацией о соревнованиях

### 6. Что нам нужно найти

Пожалуйста, предоставьте мне:

1. **URL запроса** (Request URL)
   ```
   Пример: https://timerman.org/api/v1/events
   ```

2. **Метод** (GET или POST)
   ```
   Пример: POST
   ```

3. **Тело запроса** (Request Payload, если POST)
   ```json
   Пример:
   {
     "skip": 0,
     "take": 50,
     "filters": {}
   }
   ```

4. **Пример ответа** (Response)
   ```json
   Пример:
   {
     "events": [
       {
         "id": "123",
         "name": "Казанский марафон",
         "date": "2025-05-03",
         "city": "Казань",
         ...
       }
     ],
     "total": 100
   }
   ```

### 7. Дополнительная информация

Также было бы полезно узнать:
- Есть ли пагинация? (параметры skip/take, page/limit и т.д.)
- Есть ли фильтры? (по городу, дате, виду спорта)
- Какие поля содержит каждое соревнование?

---

## После получения данных

Когда вы предоставите мне эту информацию, я:
1. Обновлю `timerman_parser.py` с правильными endpoints и структурой
2. Адаптирую парсинг данных под реальную структуру API
3. Протестирую получение соревнований

---

## Альтернативный способ (если не получается через браузер)

Если через браузер сложно найти, можно:
1. Связаться с поддержкой Timerman и спросить есть ли у них публичный API
2. Проверить их Telegram канал (https://t.me/timerman_org) - возможно там есть информация
3. Посмотреть в исходном коде страницы (Ctrl+U) - возможно есть встроенные данные

---

## Что делать сейчас?

**Вариант 1:** Найдите API endpoint через браузер и предоставьте мне информацию выше

**Вариант 2:** Если API недоступен, мы можем:
- Временно отключить парсер Timerman (только RussiaRunning будет работать)
- Или создать заглушку которая показывает, что данные скоро будут доступны

**Вариант 3:** Использовать парсинг HTML через Selenium/Playwright (более сложный вариант)
