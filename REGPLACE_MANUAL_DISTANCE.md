# Ручной ввод дистанции для reg.place - 19 декабря 2025

## Дата: 2025-12-19

## Задачи

1. Сделать чтобы пользователь самостоятельно вводил дистанцию на соревнованиях от reg.place, как у Лиги Героев
2. Показывать кнопку "Добавить дистанцию" если пользователь уже зарегистрирован, иначе "Я участвую"

## Решение

### 1. Ручной ввод дистанции (строки 790-792)

Добавлен reg.place в условие для ручного ввода дистанции.

**Было:**
```python
if service == 'HeroLeague':
    # Для HeroLeague просим пользователя ввести дистанцию вручную
    await state.set_state(UpcomingCompetitionsStates.waiting_for_custom_distance)
```

**Стало:**
```python
if service in ('HeroLeague', 'reg.place'):
    # Для HeroLeague и reg.place просим пользователя ввести дистанцию вручную
    await state.set_state(UpcomingCompetitionsStates.waiting_for_custom_distance)
```

### 2. Кнопки в детальной информации (строки 646-657)

Добавлен reg.place в логику отображения кнопок "Я участвую" / "Добавить дистанцию".

**Было:**
```python
if service == 'HeroLeague' and sport_code != 'camp':
    # Для спортивных событий HeroLeague показываем "Добавить дистанцию"
    if is_participant:
        builder.row(InlineKeyboardButton(text="➕ Добавить дистанцию", ...))
    else:
        builder.row(InlineKeyboardButton(text="✅ Я участвую", ...))
```

**Стало:**
```python
if (service == 'HeroLeague' and sport_code != 'camp') or service == 'reg.place':
    # Для спортивных событий HeroLeague и reg.place показываем "Добавить дистанцию"
    if is_participant:
        builder.row(InlineKeyboardButton(text="➕ Добавить дистанцию", ...))
    else:
        builder.row(InlineKeyboardButton(text="✅ Я участвую", ...))
```

## Результат

Теперь reg.place работает **ТОЧНО ТАК ЖЕ** как другие парсеры (RussiaRunning, Timerman, HeroLeague):

### В детальной информации соревнования:
- ✅ Если пользователь **НЕ** зарегистрирован → кнопка **"✅ Я участвую"**
- ✅ Если пользователь **УЖЕ** зарегистрирован → кнопка **"➕ Добавить дистанцию"**

### Процесс регистрации:
1. ✅ Пользователь нажимает "Я участвую" или "Добавить дистанцию" в детальной информации
2. ✅ Пользователю предлагается **вручную ввести дистанцию** (как в HeroLeague)
3. ✅ Пользователь вводит дистанцию (например: "5 км", "10 км", "Марафон")
4. ✅ Затем пользователю предлагается ввести целевое время
5. ✅ После ввода времени пользователь перенаправляется в раздел "✅ Мои соревнования"

## Поведение идентично HeroLeague

Теперь reg.place работает **точно так же** как HeroLeague:
- Нет предопределенных дистанций для выбора
- Пользователь сам вводит дистанцию
- Система принимает любой формат (числа с единицами измерения, текст типа "Марафон")
- Дистанция сохраняется в базу вместе с целевым временем

## Файлы изменены

1. **`competitions/upcoming_competitions_handlers.py`**
   - **Строки 790-792**: Изменено условие для ручного ввода дистанции - добавлен reg.place
   - **Строки 646-657**: Изменена логика отображения кнопок - добавлен reg.place
   - Обновлены комментарии для упоминания reg.place

## Тестирование

### Сценарий 1: Первая регистрация
1. Выберите сервис "reg.place"
2. Найдите соревнование (например, пробег или велогонку)
3. Откройте детальную информацию
4. Убедитесь что показывается кнопка **"✅ Я участвую"**
5. Нажмите "Я участвую"
6. Убедитесь что появился запрос на ввод дистанции (📏 **Введите дистанцию**)
7. Введите дистанцию (например: "10 км")
8. Введите целевое время
9. Проверьте что попали в "✅ МОИ СОРЕВНОВАНИЯ" и там есть ваше соревнование

### Сценарий 2: Добавление еще одной дистанции
1. Откройте то же соревнование снова
2. Убедитесь что теперь показывается кнопка **"➕ Добавить дистанцию"**
3. Нажмите "Добавить дистанцию"
4. Введите другую дистанцию (например: "5 км")
5. Введите целевое время
6. Проверьте что в "✅ МОИ СОРЕВНОВАНИЯ" теперь есть 2 записи с разными дистанциями

---

✅ **Все задачи выполнены!**
