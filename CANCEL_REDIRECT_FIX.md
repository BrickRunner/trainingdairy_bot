# Исправление редиректа при отмене действий - 19 декабря 2025

## Дата: 2025-12-19

## Проблема

При нажатии кнопки "❌ Отмена" в процессе изменения целевого времени пользователя перенаправляло в главное меню соревнований вместо возврата к детальной информации о событии.

## Решение

Изменена логика редиректа при отмене изменения целевого времени.

### Исправление в process_target_time_edit() (строки 1173-1368)

**Было:**
```python
if message.text == "❌ Отмена":
    await state.clear()
    await message.answer(
        "❌ Изменение целевого времени отменено",
        reply_markup=ReplyKeyboardRemove()
    )
    # Возврат в меню соревнований
    await message.answer(
        "🏆 <b>СОРЕВНОВАНИЯ</b>\n\n"
        "Выберите раздел:",
        parse_mode="HTML",
        reply_markup=get_competitions_main_menu()
    )
    return
```

**Стало:**
```python
if message.text == "❌ Отмена":
    # Получаем данные из состояния
    data = await state.get_data()
    competition_id = data.get('edit_target_comp_id')
    distance = data.get('edit_target_distance')
    user_id = message.from_user.id

    await state.clear()
    await message.answer(
        "❌ Изменение целевого времени отменено",
        reply_markup=ReplyKeyboardRemove()
    )

    # Возврат к детальной информации о событии
    if competition_id is not None:
        # Получаем информацию о соревновании
        competition = await get_competition(competition_id)

        if competition:
            # Получаем данные участия пользователя
            from competitions.competitions_queries import get_user_competitions
            user_comps = await get_user_competitions(user_id)

            # Находим нужную регистрацию (с учетом distance=0/None для reg.place/HeroLeague)
            registration = None
            for comp in user_comps:
                comp_distance = comp.get('distance')
                if comp['id'] == competition_id:
                    if (comp_distance == distance) or \
                       (comp_distance in (None, 0) and distance in (None, 0)):
                        registration = comp
                        break

            if not registration:
                registrations_for_comp = [c for c in user_comps if c['id'] == competition_id]
                if len(registrations_for_comp) == 1:
                    registration = registrations_for_comp[0]

            if registration:
                # Форматируем детальную информацию о событии
                # (весь код формирования текста и клавиатуры)
                ...

                # ВАЖНО: Отправляем новое сообщение (а не редактируем пользовательское)
                await message.answer(
                    text,
                    reply_markup=builder.as_markup(),
                    parse_mode="HTML"
                )
                return

    # Если не удалось показать детали, возвращаемся в "Мои соревнования"
    await message.answer(
        "Вернитесь в раздел 'Мои соревнования' для просмотра событий.",
        reply_markup=get_main_menu_keyboard()
    )
    return
```

**Ключевые изменения:**
1. Вместо вызова `view_my_competition()` через FakeCallback, код формирует сообщение напрямую
2. Используется `message.answer()` вместо `callback.message.edit_text()`, т.к. сообщение от пользователя нельзя редактировать
3. Добавлена полная логика поиска регистрации, нормализации дистанции и форматирования информации

## Результат

✅ **При отмене изменения целевого времени** - пользователь возвращается к детальной информации о событии:
- Нажатие "❌ Отмена" при вводе нового времени → показывается детальная информация о событии
- Пользователь видит всю информацию о соревновании и может выбрать другое действие

✅ **При подтверждении отмены регистрации** - пользователь попадает в "Мои соревнования":
- После отмены регистрации событие больше не в списке
- Правильно показывается список оставшихся зарегистрированных событий

## Файлы изменены

**competitions/competitions_handlers.py**
- **Строки 1173-1368**: `process_target_time_edit()` - изменен редирект при отмене на возврат к детальной информации о событии
  - Вместо FakeCallback используется прямое формирование сообщения
  - Используется `message.answer()` вместо `edit_text()` для корректной работы
  - Добавлена полная логика поиска регистрации и форматирования

## Тестирование

### Сценарий 1: Отмена изменения целевого времени
1. Откройте "✅ Мои соревнования"
2. Откройте детальную информацию о любом соревновании
3. Нажмите "✏️ Изменить целевое время"
4. Нажмите кнопку "❌ Отмена"
5. ✅ Должна открыться детальная информация о том же соревновании

### Сценарий 2: Подтверждение отмены регистрации
1. Откройте "✅ Мои соревнования"
2. Откройте детальную информацию о любом соревновании
3. Нажмите "❌ Отменить участие"
4. Нажмите "✅ Да, отменить"
5. ✅ Должно открыться "✅ МОИ СОРЕВНОВАНИЯ" (список оставшихся соревнований)

### Сценарий 3: Отказ от отмены регистрации
1. Откройте "✅ Мои соревнования"
2. Откройте детальную информацию о любом соревновании
3. Нажмите "❌ Отменить участие"
4. Нажмите "❌ Нет, вернуться"
5. ✅ Должна открыться детальная информация о том же соревновании

---

✅ **Проблема решена!**

**Теперь при отмене действий пользователь возвращается к детальной информации о событии**
