# Исправление ошибки "message can't be edited" - 19 декабря 2025

## Дата: 2025-12-19

## Проблема

При нажатии кнопки "❌ Отмена" в процессе изменения целевого времени появлялась ошибка:

```
aiogram.exceptions.TelegramBadRequest: Telegram server says - Bad Request: message can't be edited
```

## Причина

В предыдущей реализации при отмене изменения целевого времени использовался подход с FakeCallback:

```python
class FakeCallback:
    def __init__(self, msg):
        self.message = msg  # Это сообщение пользователя "❌ Отмена"
        self.from_user = msg.from_user
        self.data = f"comp:my_view:{competition_id}:{distance if distance else 0}"

    async def answer(self, text="", show_alert=False):
        pass

fake_callback = FakeCallback(message)
await view_my_competition(fake_callback, state)
```

Проблема в том, что:
1. `message` - это текстовое сообщение от пользователя ("❌ Отмена")
2. Функция `view_my_competition()` вызывает `callback.message.edit_text()`
3. **Telegram не позволяет редактировать сообщения пользователя** - можно редактировать только сообщения бота

**Результат:** ошибка "message can't be edited"

## Решение

Вместо вызова `view_my_competition()` через FakeCallback, теперь код формирует и отправляет **новое сообщение** с детальной информацией о событии.

### Реализация (строки 1173-1368)

```python
if message.text == "❌ Отмена":
    # Получаем данные из состояния
    data = await state.get_data()
    competition_id = data.get('edit_target_comp_id')
    distance = data.get('edit_target_distance')
    user_id = message.from_user.id

    await state.clear()
    await message.answer(
        "❌ Изменение целевого времени отменено",
        reply_markup=ReplyKeyboardRemove()
    )

    # Возврат к детальной информации о событии
    if competition_id is not None:
        # Получаем информацию о соревновании
        competition = await get_competition(competition_id)

        if competition:
            # Получаем данные участия пользователя
            from competitions.competitions_queries import get_user_competitions
            user_comps = await get_user_competitions(user_id)

            # Находим нужную регистрацию
            registration = None
            for comp in user_comps:
                comp_distance = comp.get('distance')
                if comp['id'] == competition_id:
                    if (comp_distance == distance) or \
                       (comp_distance in (None, 0) and distance in (None, 0)):
                        registration = comp
                        break

            if not registration:
                registrations_for_comp = [c for c in user_comps if c['id'] == competition_id]
                if len(registrations_for_comp) == 1:
                    registration = registrations_for_comp[0]

            if registration:
                # Форматируем информацию о событии
                # (код нормализации distance_name, форматирования текста и клавиатуры)
                ...

                # ВАЖНО: Отправляем новое сообщение (а не редактируем)
                await message.answer(
                    text,
                    reply_markup=builder.as_markup(),
                    parse_mode="HTML"
                )
                return

    # Если не удалось показать детали
    await message.answer(
        "Вернитесь в раздел 'Мои соревнования' для просмотра событий.",
        reply_markup=get_main_menu_keyboard()
    )
    return
```

## Ключевые изменения

1. **Удален FakeCallback подход** - больше не пытаемся обмануть систему
2. **Дублирован код из `view_my_competition()`** - формирование текста и клавиатуры
3. **Используется `message.answer()`** вместо `callback.message.edit_text()`
4. **Добавлена полная логика**:
   - Поиск регистрации с учетом distance=0/None для reg.place/HeroLeague
   - Нормализация distance_name
   - Форматирование дистанции с единицами измерения
   - Создание клавиатуры с кнопками действий

## Почему нельзя просто редактировать?

**Telegram API ограничения:**
- ✅ Можно редактировать сообщения, отправленные ботом
- ❌ Нельзя редактировать сообщения, отправленные пользователем

**В нашем случае:**
- `message` в обработчике `process_target_time_edit()` - это текстовое сообщение пользователя
- Когда пользователь нажимает клавиатурную кнопку "❌ Отмена", он отправляет **текстовое сообщение**
- Это сообщение от пользователя, а не от бота
- Поэтому `message.edit_text()` не работает

**Решение:** отправить новое сообщение от бота с помощью `message.answer()`

## Результат

✅ При нажатии "❌ Отмена" во время изменения целевого времени:
- Показывается сообщение "❌ Изменение целевого времени отменено"
- Отправляется **новое сообщение** с детальной информацией о событии
- Все кнопки работают корректно
- Нет ошибки "message can't be edited"

## Файлы изменены

**competitions/competitions_handlers.py**
- **Строки 1173-1368**: `process_target_time_edit()` - полностью переписан обработчик отмены
  - Удален FakeCallback подход
  - Добавлена прямая логика формирования сообщения
  - Используется `message.answer()` для отправки нового сообщения
  - Используется глобальный импорт `InlineKeyboardButton` из строки 8

- **Строки 1555-1569**: `cancel_registration()` - исправлено использование кнопок
  - Удален конфликтующий локальный импорт `InlineKeyboardButton as IKB`
  - Используется глобальный импорт `InlineKeyboardButton` из строки 8

- **Строки 852, 1339**: Переименованы callback_data для кнопок отмены регистрации
  - Изменено с `comp:cancel_registration:` на `comp:cancel_reg_ask:`
  - Это предотвращает конфликт с `comp:cancel_reg_confirm:`

- **Строка 1486**: Обновлен декоратор `cancel_registration()`
  - Изменен с `F.data.regexp(...)` на `F.data.startswith("comp:cancel_reg_ask:")`
  - Простой и надежный способ без регулярных выражений

- **Строка 1561**: Переименован callback_data для кнопки подтверждения
  - Изменено с `comp:cancel_registration_confirm:` на `comp:cancel_reg_confirm:`

- **Строка 1587**: Обновлен декоратор `confirm_cancel_registration()`
  - Изменен с `comp:cancel_registration_confirm:` на `comp:cancel_reg_confirm:`

**Важно:** Локальные импорты `InlineKeyboardButton as IKB` вызывали конфликт с глобальным импортом, что приводило к ошибке `UnboundLocalError`. Решение - использовать только глобальный импорт из начала файла.

## Тестирование

### Сценарий 1: Отмена изменения целевого времени
1. Откройте "✅ Мои соревнования"
2. Откройте детальную информацию о любом соревновании
3. Нажмите "✏️ Изменить целевое время"
4. Нажмите кнопку "❌ Отмена"
5. ✅ Должно показаться сообщение "❌ Изменение целевого времени отменено"
6. ✅ Должна открыться детальная информация о том же соревновании (новое сообщение)
7. ✅ Нет ошибки "message can't be edited"

### Сценарий 2: Отмена регистрации
1. Откройте "✅ Мои соревнования"
2. Откройте детальную информацию о соревновании
3. Нажмите "❌ Отменить участие"
4. ✅ Должно показаться подтверждение с кнопками "✅ Да, отменить" и "❌ Нет, вернуться"
5. ✅ Нет ошибки `UnboundLocalError: cannot access local variable 'InlineKeyboardButton'`

### Сценарий 3: Проверка всех кнопок после отмены
1. После отмены изменения времени (сценарий 1)
2. Проверьте что все кнопки работают:
   - ✅ "✏️ Изменить целевое время" - открывается
   - ✅ "❌ Отменить участие" - работает
   - ✅ "🌐 Официальный сайт" - открывается (если есть)
   - ✅ "◀️ Назад" - возвращает к списку

---

✅ **Проблема решена!**

**Теперь при отмене изменения целевого времени отправляется новое сообщение вместо попытки редактирования пользовательского сообщения**
