# Training Assistant - AI-Powered Training Coach

## Описание

Training Assistant — это интеллектуальный помощник тренера, использующий искусственный интеллект для:
- Создания персональных тренировочных планов
- Корректировки тренировок на основе обратной связи
- Подготовки к соревнованиям
- Построения тактики забега
- Психологической поддержки
- Прогнозирования результатов

## Архитектура

### Структура модуля

```
training_assistant/
├── __init__.py                      # Экспорт роутера
├── ta_handlers.py                   # Основные обработчики (главное меню)
├── ta_fsm.py                        # FSM состояния
├── ta_keyboards.py                  # Клавиатуры и inline-кнопки
├── ta_queries.py                    # Работа с БД
├── services/
│   ├── __init__.py
│   ├── plan_generator.py            # Сервис генерации планов тренировок
│   ├── workout_corrector.py         # Сервис коррекции тренировок
│   ├── race_preparation.py          # Подготовка к соревнованиям
│   ├── race_tactics.py              # Тактика забега
│   ├── sports_psychologist.py       # Психологическая поддержка
│   └── result_predictor.py          # Прогноз результатов
├── prompts/
│   ├── __init__.py
│   └── templates.py                 # Prompt шаблоны для каждой функции
└── README.md                        # Документация модуля
```

### База данных

Модуль использует 7 таблиц:

1. **training_plans** - Тренировочные планы
   - Хранит сгенерированные AI планы с параметрами (спорт, дистанция, уровень)
   - JSON структура с детальным расписанием

2. **training_corrections** - Корректировки
   - Обратная связь пользователя (легко/тяжело/высокий пульс)
   - AI-анализ и рекомендации

3. **race_preparations** - Подготовка к соревнованиям
   - Рекомендации за 7/5/3/1 день до старта
   - Что делать и не делать

4. **race_tactics** - Тактика забега
   - Сплиты и темпы по сегментам
   - Стратегия темпа (negative/even/positive split)

5. **ai_conversations** - Диалоги
   - История общения с AI-психологом
   - Сохранение контекста для продолжения диалога

6. **result_predictions** - Прогнозы
   - 3 варианта прогноза (реалистичный, оптимистичный, осторожный)
   - Факторы влияющие на результат

7. **ta_user_settings** - Настройки
   - Предпочтения AI-стиля
   - Статистика использования

## Функции

### 1️⃣ План тренировок

**Параметры:**
- Вид спорта (бег, плавание, велоспорт, триатлон)
- Длительность (неделя/месяц)
- Уровень подготовки (новичок, средний, продвинутый)
- Доступные дни для тренировок
- Целевая дистанция (опционально)
- Цель (опционально)

**AI анализирует:**
- Последние тренировки за месяц
- Личные рекорды
- Текущую форму

**Результат:**
- Детальный план с типом, объемом, интенсивностью каждой тренировки
- Объяснение почему такой план
- Целевые темпы и пульсовые зоны

**Пример использования:**
```python
from training_assistant.services import generate_training_plan

plan = await generate_training_plan(
    user_id=123,
    sport_type='run',
    plan_duration='week',
    fitness_level='intermediate',
    available_days=['Пн', 'Ср', 'Пт', 'Вс'],
    target_distance=21.1  # Полумарафон
)
```

### 2️⃣ Корректировка тренировки

**Обратная связь:**
- Было тяжело
- Было легко
- Пульс выше нормы
- Не уложился в темп
- Не закончил тренировку

**AI анализирует:**
- Выполненную тренировку
- Последние 5 тренировок
- Пульсовые зоны

**Результат:**
- Причины проблемы
- Рекомендации для следующих тренировок
- Корректировки плана на неделю

### 3️⃣ Подготовка к соревнованию

**Периоды:**
- За 7 дней: поддержание формы, короткие интервалы
- За 5 дней: tapering, снижение нагрузки
- За 3 дня: легкие тренировки, проверка экипировки
- За 1 день: легкая разминка, режим питания, сон

**Результат:**
- Конкретные тренировки
- Что делать / что НЕ делать
- Питание, сон, ментальная подготовка

### 4️⃣ Тактика забега

**Параметры:**
- Дистанция
- Целевое время
- Тип трассы (ровная, холмистая, трейл)

**Результат:**
- Стратегия темпа (negative split и др.)
- Сплиты по сегментам
- Советы по началу, середине, финишу
- План B если что-то пойдет не так

### 5️⃣ Спортивный психолог

**Типовые проблемы:**
- Страх перед стартом
- Сомнения в силах
- Потеря уверенности
- Боязнь не оправдать ожидания

**AI помогает:**
- Рационализировать страхи
- Дает конкретные техники
- Фокусирует на контролируемом
- Поддерживает и мотивирует

**Диалог:**
- Сохранение контекста (последние 5 сообщений)
- Эмпатичный стиль общения
- Практические советы

### 6️⃣ Прогноз результата

**Параметры:**
- Целевая дистанция
- Период анализа (месяц/2 недели)

**AI анализирует:**
- Темпы на тренировках
- Объем тренировок
- Прогрессию
- Типы тренировок

**Результат:**
- 3 прогноза (реалистичный, оптимистичный, осторожный)
- Уровень уверенности (%)
- Ключевые факторы
- Рекомендации для улучшения

## Использование

### Команды

```
/ai_assistant - Открыть меню Training Assistant
/ta - Короткая команда
```

### Кнопка в главном меню

Кнопка "🤖 Training Assistant" доступна в главном меню бота

### Пример flow

1. Пользователь: нажимает "План тренировок"
2. Бот: выбирает вид спорта → длительность → уровень → дни
3. AI: анализирует данные пользователя и генерирует план
4. Бот: выводит детальный план с объяснениями
5. План сохраняется в БД

## Настройка

### Требования

- Python 3.10+
- aiogram 3.x
- openai library
- aiosqlite

### Переменные окружения

```bash
# .env
OPENROUTER_API_KEY=your_api_key_here
```

Получить ключ: https://openrouter.ai/

### Модель AI

По умолчанию используется `google/gemini-2.5-flash` (бесплатная, стабильная).

Можно изменить в каждом сервисе:
```python
response = await _call_with_retry(
    ai_client,
    model="google/gemini-2.5-flash",  # Изменить здесь
    messages=[...],
    temperature=0.7,
    max_tokens=1500
)
```

**Другие бесплатные модели OpenRouter:**
- `google/gemini-2.5-flash` - рекомендуется (быстрая, качественная)
- `google/gemini-2.5-flash-lite` - облегченная версия
- `google/gemini-3-flash-preview-20251217` - preview версия с новыми возможностями
- `arcee-ai/trinity-large-preview` - альтернатива для creative writing

Проверить доступные модели: https://openrouter.ai/models?order=newest&supported_parameters=tools

## Prompts

Все промпты находятся в `prompts/templates.py`.

### System Prompts

- `SYSTEM_PROMPT_COACH` - Для тренерских функций
- `SYSTEM_PROMPT_PSYCHOLOGIST` - Для психолога

### Function Prompts

- `PROMPT_TRAINING_PLAN` - Генерация плана
- `PROMPT_CORRECTION` - Коррекция
- `PROMPT_RACE_PREPARATION` - Подготовка к соревнованию
- `PROMPT_RACE_TACTICS` - Тактика
- `PROMPT_PSYCHOLOGIST` - Психолог
- `PROMPT_RESULT_PREDICTION` - Прогноз

### Кастомизация

Промпты можно настраивать под свои нужды:

```python
# Пример кастомизации стиля
SYSTEM_PROMPT_COACH = """Ты профессиональный тренер...
Стиль: дружелюбный / профессиональный / мотивационный
"""
```

## Расширение

### Добавление новой функции

1. Создайте сервис в `services/new_service.py`
2. Добавьте FSM состояния в `ta_fsm.py`
3. Добавьте обработчики в `ta_handlers.py`
4. Создайте промпт в `prompts/templates.py`
5. Добавьте таблицу БД в `database/models.py` (если нужно)
6. Добавьте queries в `ta_queries.py`

### Интеграция с другими модулями

```python
# Использование в других частях бота
from training_assistant.services import generate_training_plan

# В вашем обработчике
plan = await generate_training_plan(...)
```

## Best Practices

### 1. Retry механизм

Все AI-вызовы используют retry при rate limit (429):

```python
response = await _call_with_retry(
    ai_client,
    model="...",
    messages=[...],
    temperature=0.7,
    max_tokens=1500
)
```

### 2. JSON парсинг

AI возвращает JSON, но может обернуть в ```json```:

```python
if "```json" in ai_response:
    json_start = ai_response.find("```json") + 7
    json_end = ai_response.find("```", json_start)
    json_str = ai_response[json_start:json_end].strip()
else:
    json_str = ai_response

data = json.loads(json_str)
```

### 3. Обработка ошибок

Всегда проверяйте результат AI:

```python
result = await some_ai_service(...)
if not result:
    await message.answer("Ошибка AI. Попробуйте позже.")
    return
```

### 4. Сохранение в БД

Сохраняйте все AI-ответы для аналитики:

```python
await save_training_plan(user_id, plan_type, plan_content, ...)
```

## Troubleshooting

### Ошибка: "AI client not configured"

Проверьте `OPENROUTER_API_KEY` в `.env`

### Rate limit (429)

Встроенный retry механизм автоматически повторит запрос через 3, 6, 12 секунд.

### JSON parsing errors

AI иногда возвращает текст вместо JSON. Код обрабатывает это:

```python
except json.JSONDecodeError:
    return {"analysis": ai_response, "raw_response": ai_response}
```

### Длинные ответы

Telegram ограничивает сообщения 4096 символами:

```python
response[:4000]  # Обрезаем если нужно
```

## Лицензия

Часть проекта trainingdairy_bot

## Контакты

- Вопросы: создайте issue в репозитории
- Предложения: pull request приветствуются
